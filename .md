flow bikin service baru tetap bisa dipakai, cuma penyesuaian kecil di folder FE:

📁 Frontend (React Router v7)

src/
├── types/                 → interface, enum
│   └── {nama-fitur}.ts
│
├── services/              → API client (pake axios / createApi)
│   └── {namaFitur}Api.ts
│
├── hooks/                 → custom hook (useXxx)
│   └── use-{nama-fitur}.ts
│
└── routes/ atau components/ → UI component & route

✅ createApi tetap bisa dipakai (kayak di sequenceApi lu)
✅ Hook tetap jalan karena pake useState, useCallback, dll (React standar)


FE (hook) → service → API (Express) → controller → service → Prisma → DB